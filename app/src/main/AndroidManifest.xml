<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shq.subjecttimetable">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.CourseTableActivity" />
        <activity android:name=".activities.TestFragment" />
        <activity android:name=".activities.CourseActivity" />
        <activity android:name=".F4" />
        <activity android:name=".activities.fragment" />
        <activity android:name=".Web1" />
        <activity android:name=".F2" />
        <activity android:name=".showScore" />
        <activity android:name=".F3" />
    </application>

</manifest>